import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "images.unsplash.com",
      "variety.com",
      "sm.ign.com",
      "platform.polygon.com",
      "static.bandainamcoent.eu",
    ],
  },
};

export default nextConfig;
